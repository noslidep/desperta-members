import {getEvents} from '../../../lib/data'
export default async function Agenda(){const events=await getEvents();return <div className="page"><div className="section-head"><div><span className="eyebrow">Encontros</span><h3 style={{fontSize:30}}>Agenda</h3><p>Lives, mentorias, plantões e eventos da sua jornada.</p></div></div><div className="grid grid-2">{events.map(e=><article className="card card-pad" key={e.id}><span className="badge">{new Date(e.starts_at).toLocaleDateString('pt-BR',{day:'2-digit',month:'short'}).toUpperCase()}</span><h3 style={{color:'#071b3e'}}>{e.title}</h3><p className="muted">{new Date(e.starts_at).toLocaleString('pt-BR',{dateStyle:'full',timeStyle:'short'})}</p><p className="muted">{e.location_label}</p><button className="btn btn-primary">Adicionar à agenda</button></article>)}</div>{!events.length&&<div className="card empty">Nenhum encontro agendado.</div>}</div>}
