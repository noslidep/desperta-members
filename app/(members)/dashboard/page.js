import Image from 'next/image';import Link from 'next/link';import ProgramCard from '../../../components/ProgramCard';import ProgressBar from '../../../components/ProgressBar';import {getCurrentProfile,getMyPrograms,getEvents,getMaterials,getAnnouncements} from '../../../lib/data'
export default async function Dashboard(){const[profile,programs,events,materials,ann]=await Promise.all([getCurrentProfile(),getMyPrograms(),getEvents(),getMaterials(),getAnnouncements()]);const active=programs.find(p=>p.progress>0&&p.progress<100)||programs[0];return <div className="page"><section className="hero"><div className="hero-copy"><span className="eyebrow">Sua jornada Desperta</span><h2>Olá, {profile?.full_name?.split(' ')[0]||'Aluna'}.</h2><p>Continue de onde parou, acompanhe seus encontros e encontre tudo que precisa para avançar com direção e constância.</p><div className="actions">{active&&<Link className="btn btn-primary" href={`/programas/${active.slug}`}>▶ Continuar estudando</Link>}<Link className="btn btn-ghost" href="/programas">Ver meus programas</Link></div></div><div className="hero-person"><Image src="/vanessa.png" width={560} height={760} alt="Vanessa" priority/></div></section>{active&&<section className="section"><div className="section-head"><div><h3>Continuar de onde parou</h3><p>Sua próxima ação está aqui.</p></div></div><div className="card continue-card"><Image src={active.cover_url||'/formula-gestao-vendas.png'} width={500} height={400} alt={active.title}/><div className="continue-body"><span className="eyebrow">Em andamento</span><h4>{active.title}</h4><p className="muted">{active.subtitle}</p><ProgressBar value={active.progress}/><div style={{display:'flex',justifyContent:'space-between',marginTop:7,fontSize:12,color:'#66758d'}}><span>{active.progress}% concluído</span><span>{active.completed_lessons}/{active.total_lessons} aulas</span></div><div className="actions"><Link className="btn btn-primary" href={`/programas/${active.slug}`}>Continuar curso →</Link></div></div></div></section>}<section className="section"><div className="section-head"><div><h3>Meus programas</h3><p>Conteúdos liberados para sua conta.</p></div><Link href="/programas" className="badge">Ver todos →</Link></div><div className="grid grid-3">{programs.slice(0,3).map(p=><ProgramCard key={p.id} program={p}/>)}</div></section><section className="section grid grid-3"><div className="card stat"><strong>{programs.filter(p=>p.enrolled).length}</strong><span>programas ativos</span></div><div className="card stat"><strong>{programs.reduce((n,p)=>n+(p.completed_lessons||0),0)}</strong><span>aulas concluídas</span></div><div className="card stat"><strong>{events.length}</strong><span>próximos encontros</span></div></section><section className="section grid grid-2"><div><div className="section-head"><div><h3>Próximos encontros</h3><p>Agenda da sua jornada.</p></div></div><div className="card list">{events.slice(0,3).map(e=><div className="list-item" key={e.id}><div className="list-icon">◷</div><div className="list-main"><strong>{e.title}</strong><span>{new Date(e.starts_at).toLocaleString('pt-BR',{dateStyle:'medium',timeStyle:'short'})} • {e.location_label}</span></div></div>)}{!events.length&&<div className="empty">Nenhum encontro agendado.</div>}<div style={{padding:14}}><Link className="btn btn-secondary" href="/agenda">Ver agenda completa</Link></div></div></div><div><div className="section-head"><div><h3>Materiais recentes</h3><p>Workbooks e ferramentas para aplicar.</p></div></div><div className="card list">{materials.slice(0,3).map(m=><div className="list-item" key={m.id}><div className="list-icon">▣</div><div className="list-main"><strong>{m.title}</strong><span>{m.program_title} • {m.file_type||'Material'}</span></div></div>)}<div style={{padding:14}}><Link className="btn btn-secondary" href="/materiais">Abrir biblioteca</Link></div></div></div></section>{ann?.[0]&&<section className="section card card-pad"><span className="eyebrow">Comunicado</span><h3 style={{color:'#071b3e'}}>{ann[0].title}</h3><p className="muted">{ann[0].body}</p></section>}</div>}
