import ProgressBar from '../../../components/ProgressBar';import {getCertificates} from '../../../lib/data'
export default async function Certificates(){const items=await getCertificates();return <div className="page"><div className="section-head"><div><span className="eyebrow">Conquistas</span><h3 style={{fontSize:30}}>Certificados</h3><p>Seus certificados são liberados conforme os critérios de conclusão.</p></div></div><div className="grid grid-2">{items.map(c=><article className="card card-pad" key={c.id}><span className="badge">{c.status==='available'?'Disponível':'Em andamento'}</span><h3 style={{color:'#071b3e'}}>{c.program_title}</h3><ProgressBar value={c.progress||0}/><p className="muted">{c.progress||0}% concluído</p>{c.status==='available'?<a className="btn btn-primary" href={c.file_url||'#'}>Baixar certificado</a>:<button className="btn btn-secondary" disabled>Conclua os requisitos para liberar</button>}</article>)}</div></div>}
