import {getCurrentProfile} from '../../../lib/data'
import {updateMyProfile} from './actions'
export default async function Account({searchParams}){const p=await getCurrentProfile();const qs=await searchParams;return <div className="page"><div className="section-head"><div><span className="eyebrow">Perfil</span><h3 style={{fontSize:30}}>Minha Conta</h3><p>Seus dados e preferências de acesso.</p></div></div>{qs?.saved&&<div className="alert alert-success">Dados atualizados com sucesso.</div>}<div className="grid grid-2"><form action={updateMyProfile} className="card form-card"><h3 style={{color:'#071b3e'}}>Dados pessoais</h3><div className="field"><label>Nome completo</label><input name="full_name" defaultValue={p?.full_name||''} required/></div><div className="field"><label>E-mail</label><input defaultValue={p?.email||''} disabled/></div><div className="field"><label>Telefone</label><input name="phone" defaultValue={p?.phone||''} placeholder="(00) 00000-0000"/></div><button className="btn btn-primary">Salvar alterações</button></form><div className="card form-card"><h3 style={{color:'#071b3e'}}>Segurança</h3><p className="muted">Sua autenticação é gerenciada pela nova Área Desperta. Nesta etapa, alterações de senha são feitas pelo fluxo seguro do Supabase.</p><h3 style={{color:'#071b3e',marginTop:28}}>Acesso</h3><p className="muted">Os conteúdos exibidos na sua conta dependem dos acessos liberados pela administração.</p></div></div></div>}
