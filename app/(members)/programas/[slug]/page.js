import {notFound} from 'next/navigation';import Image from 'next/image';import Link from 'next/link';import ProgressBar from '../../../../components/ProgressBar';import {getProgramBySlug} from '../../../../lib/data'
function dur(s){if(!s)return'';return `${Math.floor(s/60)}:${String(s%60).padStart(2,'0')}`}
export default async function Program({params}){const{slug}=await params;const data=await getProgramBySlug(slug);if(!data)notFound();const{program,modules}=data;const first=modules.flatMap(m=>m.lessons||[]).find(l=>!l.is_completed)||modules.flatMap(m=>m.lessons||[])[0];return <div className="page"><section className="course-hero"><div><span className="eyebrow">Programa</span><h2>{program.title}</h2><p style={{color:'#d5deeb',lineHeight:1.6}}>{program.subtitle||program.description}</p><div style={{maxWidth:520,marginTop:18}}><ProgressBar value={program.progress}/><div style={{display:'flex',justifyContent:'space-between',fontSize:12,color:'#cbd5e2',marginTop:7}}><span>{program.progress}% concluído</span><span>{program.completed_lessons}/{program.total_lessons} aulas</span></div></div>{first&&<div className="actions"><Link className="btn btn-primary" href={`/aulas/${first.id}`}>▶ Continuar curso</Link><Link className="btn btn-ghost" href="/materiais">Materiais do programa</Link></div>}</div><div className="course-logo"><Image src={program.cover_url||'/formula-gestao-vendas.png'} width={420} height={300} alt={program.title}/></div></section><section className="section"><div className="section-head"><div><h3>Conteúdo do programa</h3><p>Avance no seu ritmo. Seu progresso fica salvo automaticamente.</p></div></div><div className="grid">{modules.map((m,i)=><article className="card module" key={m.id}><div className="module-head"><div><span className="eyebrow">Módulo {i+1}</span><h4>{m.title.replace(/^Módulo \d+ — /,'')}</h4></div><span className="badge">{(m.lessons||[]).filter(l=>l.is_completed).length}/{(m.lessons||[]).length} concluídas</span></div>{(m.lessons||[]).map((l,j)=><div className={`lesson ${l.is_completed?'done':''} ${l.is_current?'current':''}`} key={l.id}><div className="lesson-status">{l.is_completed?'✓':String(j+1).padStart(2,'0')}</div><div className="lesson-title"><strong>{l.title}</strong><span>{dur(l.duration_seconds)} {l.is_current?'• Continue daqui':''}</span></div><Link className="btn btn-secondary" href={`/aulas/${l.id}`}>{l.is_completed?'Rever':'Assistir'} →</Link></div>)}</article>)}</div></section></div>}
