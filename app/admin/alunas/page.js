import {inviteStudent} from './actions';import {isDemo,createClient} from '../../../lib/supabase/server';import {demoProfile} from '../../../lib/demo'
export default async function Students(){let rows=[];if(isDemo())rows=[demoProfile,{id:'2',full_name:'Ana Souza',email:'ana@exemplo.com',role:'student'}];else{const s=await createClient();const{data}=await s.from('profiles').select('id,full_name,email,role,created_at').order('created_at',{ascending:false}).limit(100);rows=data||[]}return <div className="page"><div className="section-head"><div><span className="eyebrow">Acessos</span><h3 style={{fontSize:30}}>Alunas</h3><p>Convide alunas e gerencie acessos da plataforma.</p></div></div><div className="grid grid-2"><form action={inviteStudent} className="card form-card"><h3 style={{color:'#071b3e'}}>Convidar nova aluna</h3><div className="field"><label>Nome</label><input name="full_name" placeholder="Nome completo"/></div><div className="field"><label>E-mail</label><input name="email" type="email" placeholder="aluna@email.com" required/></div><button className="btn btn-primary">Enviar convite</button><p className="footer-note">Em produção, o Supabase envia um convite seguro por e-mail. A service role fica somente no servidor.</p></form><div className="card table-wrap"><table className="table"><thead><tr><th>Nome</th><th>E-mail</th><th>Perfil</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.full_name||'—'}</td><td>{r.email}</td><td>{r.role}</td></tr>)}</tbody></table></div></div></div>}
