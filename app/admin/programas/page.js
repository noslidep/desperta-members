import {getAllProgramsAdmin} from '../../../lib/data'
export default async function AdminPrograms(){const rows=await getAllProgramsAdmin();return <div className="page"><div className="section-head"><div><span className="eyebrow">Conteúdo</span><h3 style={{fontSize:30}}>Programas</h3><p>Cadastro e organização da estrutura educacional.</p></div><button className="btn btn-primary">+ Novo programa</button></div><div className="card table-wrap"><table className="table"><thead><tr><th>Programa</th><th>Status</th><th>Aulas</th><th>Ação</th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td><strong>{p.title}</strong><br/><small>{p.slug}</small></td><td>{p.status}</td><td>{p.total_lessons||0}</td><td><button className="btn btn-secondary">Editar</button></td></tr>)}</tbody></table></div><p className="footer-note">Nesta V1 os botões administrativos são a interface-base. O schema já está preparado para CRUD real na próxima etapa.</p></div>}
